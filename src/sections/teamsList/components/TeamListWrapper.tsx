import { TeamList } from "./TeamList";
import { TeamListHeading } from "./TeamListHeading";

export const TeamListWrapper = () => {
  
    return (
        <section>
            <TeamListHeading/>
            <TeamList/>
        </section>
    );
}